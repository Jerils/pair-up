<div class="container">
  <div class="row">
    <div class="col-md-4">
      <h1>All students</h1></br>
      <% @users.each do |user| %>
      <p>Email: <%= user.email %> Admin: <%= user.admin %> <%= link_to " Promote" %></p>
      <% end %>
    </div>


    <div class="col-md-4">
      <h1>Pairs: today</h1></br>
      <% @users.shuffle.each do |user| %>
      <%= user.email %>
      <% end %>
    </div>

    <div class="col-md-4">
      <h1>Pairs: tomorrow</h1></br>
    </div>
  </div>
</div>
